version: '3.8'

services:
  rabbitmq:
    image: rabbitmq:3.10-management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"

  send:
    build:
      context: ./send
    depends_on:
      - rabbitmq

  receive:
    build:
      context: ./receive
    depends_on:
      - rabbitmq
